export interface ThreadData {
  created_time: {
    seconds: any;
    nanoseconds: any;
  };
  user: {
    user_name: string;
  };
  body: string;
  id: string;
  image: string;
  owner_id: string;
}
